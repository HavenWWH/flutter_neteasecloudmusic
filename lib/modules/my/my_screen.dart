import 'package:flutter/material.dart';

class My_screen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My_screen'),),
    );
  }
}